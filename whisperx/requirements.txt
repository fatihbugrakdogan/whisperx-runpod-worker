# RunPod serverless SDK
runpod>=1.7.0

# WhisperX pulls in faster-whisper, transformers, etc.
# pyannote removed — Phase 3 uses NeMo (via whisper-diarization) for diarization
whisperx>=3.1.1

# Make sure we get the right ctranslate2 build for CUDA 12.x
ctranslate2>=4.0.0

# Audio enhancement — noise/echo reduction before diarization
noisereduce>=3.0.0
scipy>=1.11.0

# Phase-1 preprocessing: DeepFilterNet denoising + loudness normalization
deepfilternet>=0.5.6
pyloudnorm>=0.1.1
